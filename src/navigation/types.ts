
export interface FolderIndexSettings {
	enabled: boolean;
	hideIndexInExplorer: boolean;
}

export const DEFAULT_FOLDER_INDEX_SETTINGS: FolderIndexSettings = {
	enabled: true,
	hideIndexInExplorer: false,
};

