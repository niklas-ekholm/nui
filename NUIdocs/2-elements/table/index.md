# Table

Native Obsidian Bases table styling — not an NUI Plugin layout type, but part of the visual system.

## In this section

- [[2-elements/table/implementation|Implementation]] — Native Bases table view styled by theme.css rules.

## Purpose

Theme-level styling for Obsidian's native Bases table view — documented here because it shares NUI visual language, not because NUI Plugin registers a table layout.

## Appearance

Rows are files; columns are properties. Hairline column borders; tracked caps header row; no outer frame.

## Examples

- **Table** view alongside custom views in the same `.base` file (e.g. Weekly Habits).

## Roadmap

- Tie to `theme.css` Bases sections with concrete selectors

# Elements

* [Implementation](implementation.md) - Native Bases table view styled by theme.css rules.
